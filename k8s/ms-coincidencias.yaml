apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-coincidencias
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-coincidencias
  template:
    metadata:
      labels:
        app: ms-coincidencias
    spec:
      containers:
        - name: ms-coincidencias
          image: ECR_REGISTRY/devows-project-ms-coincidencias:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          env:
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: DB_HOST
            - name: SPRING_RABBITMQ_HOST
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: DB_HOST
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://$(DB_HOST):5432/dnf_db"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: ms-coincidencias
spec:
  selector:
    app: ms-coincidencias
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
