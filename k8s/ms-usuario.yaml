apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-usuario
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-usuario
  template:
    metadata:
      labels:
        app: ms-usuario
    spec:
      containers:
        - name: ms-usuario
          image: 270613068652.dkr.ecr.us-east-1.amazonaws.com/devows-project-ms-usuario:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          env:
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: DB_HOST
            - name: SPRING_RABBITMQ_HOST
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: DB_HOST
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://$(DB_HOST):5432/dnf_db"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: ms-usuario
spec:
  selector:
    app: ms-usuario
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
